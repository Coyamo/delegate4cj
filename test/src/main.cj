package delegateTest

import delegate.macros.*
import std.collection.*
import std.reflect.*
import std.ast.*
import std.sync.AtomicReference
import std.random.*

main(): Int64 {
    println("hello world")
    return 0
}
